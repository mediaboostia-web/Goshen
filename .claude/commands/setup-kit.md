---
description: Bootstrap dev environment from zero (Node, pnpm, Neon, .env.local, skills, smoke test)
---

Invoke the `setup-kit` skill via the `Skill` tool now.

The skill lives at [.claude/skills/setup-kit/SKILL.md](.claude/skills/setup-kit/SKILL.md). It audits the user's environment, auto-installs what is automatable, and surfaces explicit paste-ready commands for every human gate (Neon signup, `/plugin install …`, API keys).

Beginner-friendly French by default. Switch to English only if the user replies in English.
